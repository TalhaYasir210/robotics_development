planner_server:
  ros__parameters:
    expected_planner_frequency: 20.0
    use_sim_time: True
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "dynamic_obstacle_avoidance::Nav2CustomPlanner"
      # You can add custom parameters here later if you want to tune A*/RRT from this file!
